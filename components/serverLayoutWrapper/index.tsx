// import { Locale } from "../../i18n-config";
// import { getDictionary } from "../../get-dictionary";
// // ------------

// const ServerLayoutWrapper = async ({ params: { lang } }: { params: { lang: Locale } }) => {
//   const dictionary = await getDictionary(lang)